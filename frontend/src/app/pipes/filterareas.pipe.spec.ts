import { FilterareasPipe } from './filterareas.pipe';

describe('FilterareasPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterareasPipe();
    expect(pipe).toBeTruthy();
  });
});
